# (c) Copyright 2024 Hewlett Packard Enterprise Development LP
name: webhook_trigger
 
permissions: write-all
     
on:
  repository_dispatch:
    types:
      - contract_requiring_verification_published
     
jobs:
  contract-test-webhook:
    name: Build
    uses: hpe-cds/reusable-workflows/.github/workflows/ci-contract-test-webhook.yml@main
    secrets: inherit